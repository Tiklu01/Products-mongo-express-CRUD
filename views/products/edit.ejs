<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Page:</title>
</head>
<body>
    <form action="/products/<%= product._id %>?_method=PUT" method="POST">
        <label for="product-name">Product Name:</label>
        <input type="text" name="name" id="product-name" placeholder="<%= product.name %>" required>
        <br>
        <label for="product-price">Price:</label>
        <input type="number" name="price" id="product-price" required min="0" step="0.01" placeholder="<%= product.price %>" >
        <br>
        <label for="product-category">Category:</label>
        <select name="category" id="product-category" aria-placeholder="<%= product.category %>">
            <option value="fruit">Fruit</option>
            <option value="vegetable">Vegetable</option>
            <option value="dairy">Dairy</option>
        </select>
        <br>
        <button type="submit">Submit</button>
    </form>
    <a href="/products"><button>Back</button></a>
</body>
</html>
